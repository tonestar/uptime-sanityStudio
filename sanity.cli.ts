import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'cyxit282',
    // dataset: 'your-dataset-name',
  },
  autoUpdates: true,
  studioHost: 'uptime',
})
